{"pageProps":{"article":{"blog_id":"3220132c-99f8-4c68-ab7b-0e4b1f942065","title":"Building a Web3 DApp A Detailed Step-by-Step Guide with Code Samples","short_description":"Web3 decentralized applications (DApps) combine blockchain-based smart contracts, data indexing, off-chain storage, and user-friendly frontends to create powerful, decentralized experiences. In this comprehensive guide, we’ll walk through the full workflow for building a Web3 DApp, using an e-commerce escrow system as an example. We’ll provide detailed explanations and sample code for each step to make the process interactive and actionable. Whether you’re a beginner or an experienced developer, this guide will help you understand how to build a DApp from scratch.","description":"<p>Web3 decentralized applications (DApps) combine blockchain-based smart contracts, data indexing, off-chain storage, and user-friendly frontends to create powerful, decentralized experiences. In this comprehensive guide, we’ll walk through the full workflow for building a Web3 DApp, using an <strong>e-commerce escrow system</strong> as an example. We’ll provide detailed explanations and sample code for each step to make the process interactive and actionable. Whether you’re a beginner or an experienced developer, this guide will help you understand how to build a DApp from scratch.</p><h1>Step 1: Smart Contract (On-Chain Logic)</h1><p>The <strong>smart contract</strong> is the core of your DApp, defining the decentralized logic that runs on a blockchain like Ethereum. For our e-commerce escrow DApp, the contract will handle order creation, payment escrow, and release of funds.</p><p><strong>What You’ll Do</strong></p><p>- Write a Solidity smart contract to manage escrow logic.</p><p>- Deploy it to a testnet (e.g., Sepolia) using Foundry.</p><p>- The contract will emit events for key actions (e.g., OrderCreated) and have a public address (e.g., 0x...).</p><h2><strong>Sample Code: EcommerceEscrow.sol</strong></h2><p>Below is a simple Solidity contract for an e-commerce escrow system. It allows buyers to create orders, holds funds in escrow, and releases them to the seller upon confirmation.</p><p>// SPDX-License-Identifier: MIT pragma solidity ^0.8.20;</p><p>contract EcommerceEscrow { // Struct to store order details struct Order { uint256 id; address buyer; address seller; uint256 amount; bool isReleased; bool isDisputed; }</p><div><pre><code>// Mapping to store orders\nmapping(uint256 =&gt; Order) public orders;\nuint256 public orderCount;\n\n// Events for indexing\nevent OrderCreated(uint256 indexed orderId, address indexed buyer, address indexed seller, uint256 amount);\nevent FundsReleased(uint256 indexed orderId, address indexed seller);\nevent DisputeRaised(uint256 indexed orderId);\n\n// Create a new order\nfunction createOrder(address _seller) external payable {\n&nbsp; &nbsp; require(msg.value &gt; 0, \"Payment must be greater than 0\");\n&nbsp; &nbsp; orderCount++;\n&nbsp; &nbsp; orders[orderCount] = Order(orderCount, msg.sender, _seller, msg.value, false, false);\n&nbsp; &nbsp; emit OrderCreated(orderCount, msg.sender, _seller, msg.value);\n}\n\n// Release funds to seller\nfunction releaseFunds(uint256 _orderId) external {\n&nbsp; &nbsp; Order storage order = orders[_orderId];\n&nbsp; &nbsp; require(msg.sender == order.buyer, \"Only buyer can release funds\");\n&nbsp; &nbsp; require(!order.isReleased, \"Funds already released\");\n&nbsp; &nbsp; require(!order.isDisputed, \"Order is disputed\");\n\n&nbsp; &nbsp; order.isReleased = true;\n&nbsp; &nbsp; payable(order.seller).transfer(order.amount);\n&nbsp; &nbsp; emit FundsReleased(_orderId, order.seller);\n}\n\n// Raise a dispute\nfunction raiseDispute(uint256 _orderId) external {\n&nbsp; &nbsp; Order storage order = orders[_orderId];\n&nbsp; &nbsp; require(msg.sender == order.buyer || msg.sender == order.seller, \"Not authorized\");\n&nbsp; &nbsp; require(!order.isReleased, \"Funds already released\");\n&nbsp; &nbsp; order.isDisputed = true;\n&nbsp; &nbsp; emit DisputeRaised(_orderId);\n}\n</code></pre></div><h2><strong>Deployment with Foundry</strong></h2><p>Install Foundry: Follow the Foundry installation guide to set up Foundry.</p><p>Compile and Deploy:</p><div><pre><code>forge create --rpc-url https://sepolia.infura.io/v3/YOUR_INFURA_KEY --private-key YOUR_PRIVATE_KEY src/EcommerceEscrow.sol:EcommerceEscrow\n</code></pre></div><p>Replace YOUR_INFURA_KEY and YOUR_PRIVATE_KEY with your credentials. This deploys the contract to Sepolia, giving it a unique address.</p><h2><strong>Why It Matters</strong></h2><p>The smart contract is your DApp’s trustless backend. It ensures that funds are securely held in escrow and only released or disputed based on predefined rules. The emitted events (OrderCreated, FundsReleased, DisputeRaised) will be indexed in the next step.</p><h1><strong>Step 2: The Graph (On-Chain Data Indexing)</strong></h1><p>Smart contracts store data and emit events, but querying this data directly from the blockchain is slow and inefficient, especially for complex data like mappings or historical events. <strong>The Graph</strong> solves this by indexing on-chain data and exposing it via a <strong>GraphQL API</strong>.</p><p><strong>What You’ll Do</strong></p><p>- Create a <strong>subgraph</strong> to listen for events from your EcommerceEscrow contract.</p><p>- Define a schema to structure the indexed data.</p><p>- Write mappings to process events and store data.</p><p>- Deploy the subgraph to The Graph’s hosted service or a decentralized network.</p><h2><strong>Sample Code: Subgraph Configuration</strong></h2><p>Below is an example of a subgraph for the EcommerceEscrow contract.</p><h3><strong>schema.graphql</strong></h3><div><pre><code>type Order @entity {\n  id: ID!\n  buyer: Bytes!\n  seller: Bytes!\n  amount: BigInt!\n  isReleased: Boolean!\n  isDisputed: Boolean!\n}\n\n\ntype OrderCreated @entity {\n  id: ID!\n  orderId: BigInt!\n  buyer: Bytes!\n  seller: Bytes!\n  amount: BigInt!\n}\n</code></pre></div><h3><strong>mapping.ts</strong></h3><div><pre><code>import { OrderCreated as OrderCreatedEvent } from \"../generated/EcommerceEscrow/EcommerceEscrow\";\nimport { Order, OrderCreated } from \"../generated/schema\";\n\n\nexport function handleOrderCreated(event: OrderCreatedEvent): void {\n  let order = new Order(event.params.orderId.toString());\n  order.buyer = event.params.buyer;\n  order.seller = event.params.seller;\n  order.amount = event.params.amount;\n  order.isReleased = false;\n  order.isDisputed = false;\n  order.save();\n\n\n  let orderCreated = new OrderCreated(event.transaction.hash.toHex() + \"-\" + event.logIndex.toString());\n  orderCreated.orderId = event.params.orderId;\n  orderCreated.buyer = event.params.buyer;\n  orderCreated.seller = event.params.seller;\n  orderCreated.amount = event.params.amount;\n  orderCreated.save();\n}\n</code></pre></div><h3><strong>subgraph.yaml</strong></h3><div><pre><code>specVersion: 0.0.5\nschema:\n  file: ./schema.graphql\ndataSources:\n  - kind: ethereum\n    name: EcommerceEscrow\n    network: sepolia\n    source:\n      address: \"YOUR_CONTRACT_ADDRESS\"\n      abi: EcommerceEscrow\n      startBlock: YOUR_START_BLOCK\n    mapping:\n      kind: ethereum/events\n      apiVersion: 0.0.7\n      language: wasm/assemblyscript\n      entities:\n        - Order\n        - OrderCreated\n      abis:\n        - name: EcommerceEscrow\n          file: ./abis/EcommerceEscrow.json\n      eventHandlers:\n        - event: OrderCreated(uint256,address,address,uint256)\n          handler: handleOrderCreated\n      file: ./src/mapping.ts\n</code></pre></div><h2><strong>Deploying the Subgraph</strong></h2><p>Install The Graph CLI:</p><div><pre><code>npm install -g @graphprotocol/graph-cli\n</code></pre></div><p>Authenticate and Deploy:</p><div><pre><code>graph auth --studio YOUR_ACCESS_TOKEN\ngraph deploy --studio YOUR_SUBGRAPH_NAME\n</code></pre></div><p>Replace YOUR_CONTRACT_ADDRESS, YOUR_START_BLOCK, YOUR_ACCESS_TOKEN, and YOUR_SUBGRAPH_NAME with your details. This deploys the subgraph, giving you a GraphQL endpoint (e.g., https://api.thegraph.com/subgraphs/name/YOUR_SUBGRAPH_NAME).</p><h2><strong>Querying the Subgraph</strong></h2><p>You can query the subgraph using GraphQL. For example:</p><div><pre><code>query {\n&nbsp;orders {\n&nbsp;&nbsp;id\n&nbsp;&nbsp;buyer\n&nbsp;&nbsp;seller\n&nbsp;&nbsp;amount\n&nbsp;&nbsp;isReleased\n&nbsp;&nbsp;isDisputed\n&nbsp;}\n}\n</code></pre></div><h2><strong>Why It Matters</strong></h2><p>The Graph makes it easy to query on-chain data like order details or transaction histories without directly interacting with the blockchain, which is slow and costly. The GraphQL API simplifies frontend development by providing structured data.</p><h1><strong>Step 3: Off-Chain Data Layer (Optional)</strong></h1><p>Blockchains are not suited for storing large or dynamic data like product images or descriptions due to high gas costs. Instead, you can use off-chain storage solutions and reference them on-chain.</p><p><strong>What You’ll Do</strong></p><p>- Store large data (e.g., product images, descriptions) on <strong>IPFS</strong>.</p><p>- Optionally, use a Web2 database like <strong>Sup Supabase</strong> for faster queries, authentication, or analytics.</p><p>- Store only a reference (e.g., IPFS CID) on the smart contract.</p><h2><strong>Example: Storing Product Data on IPFS</strong></h2><p>1. Upload to IPFS: Use a service like Pinata or Infura to upload files to IPFS. For example, upload a product image and description as a JSON file:</p><div><pre><code>{\n&nbsp;\"name\": \"Laptop\",\n&nbsp;\"description\": \"High-performance laptop\",\n&nbsp;\"image\": \"ipfs://Qm.../laptop.jpg\"\n}\n</code></pre></div><p>This generates a CID like Qm....</p><p>2. Reference in Smart Contract: Modify the Order struct in EcommerceEscrow.sol to include a metadataCID field:</p><div><pre><code>struct Order {\n&nbsp;&nbsp;uint256 id;\n&nbsp;&nbsp;address buyer;\n&nbsp;&nbsp;address seller;\n&nbsp;&nbsp;uint256 amount;\n&nbsp;&nbsp;bool isReleased;\n&nbsp;&nbsp;bool isDisputed;\n&nbsp;&nbsp;string metadataCID; // IPFS CID\n}\n</code></pre></div><p>Update the createOrder function to accept and store the CID.</p><p>3. Using Supabase (Optional): For faster queries or user authentication, set up a Supabase project and store metadata like:</p><div><pre><code>CREATE TABLE products (\n&nbsp;id SERIAL PRIMARY KEY,\n&nbsp;name TEXT,\n&nbsp;description TEXT,\n&nbsp;image_url TEXT,\n&nbsp;ipfs_cid TEXT\n);\n</code></pre></div><h2><strong>Why It Matters</strong></h2><p>Off-chain storage reduces gas costs and improves scalability. By storing only CIDs on-chain, you maintain decentralization while leveraging IPFS for data availability or Web2 databases for performance.</p><h1><strong>Step 4: Frontend (Web2 + Web3 Hybrid UI)</strong></h1><p>The frontend is where users interact with your DApp. It connects to the smart contract, subgraph, and off-chain data to provide a seamless experience.</p><p><strong>What You’ll Do</strong></p><p>- Build a frontend using <strong>Vite</strong> and <strong>React</strong>.</p><p>- Use <strong>viem</strong> to interact with the smart contract.</p><p>- Use <strong>graphql-request</strong> to query the subgraph.</p><p>- Use <strong>axios</strong> to fetch off-chain data from IPFS or Supabase.</p><h2><strong>Sample Code: React Frontend</strong></h2><p>Below is a simple React frontend using Vite to interact with the EcommerceEscrow contract and subgraph.</p><div><pre><code>import React, { useState, useEffect } from 'react';\nimport { createClient } from 'viem';\nimport { http, createWalletClient, custom } from 'viem';\nimport { sepolia } from 'viem/chains';\nimport { request } from 'graphql-request';\nimport axios from 'axios';\nimport ecommerceEscrowABI from './abis/EcommerceEscrow.json';\n\nconst client = createClient({\n  chain: sepolia,\n  transport: http('https://sepolia.infura.io/v3/YOUR_INFURA_KEY'),\n});\n\nconst walletClient = createWalletClient({\n  chain: sepolia,\n  transport: custom(window.ethereum),\n});\n\nconst contractAddress = 'YOUR_CONTRACT_ADDRESS';\nconst subgraphUrl = 'YOUR_SUBGRAPH_URL';\n\nconst App = () =&gt; {\n  const [account, setAccount] = useState(null);\n  const [orders, setOrders] = useState([]);\n  const [seller, setSeller] = useState('');\n  const [amount, setAmount] = useState('');\n  const [metadataCID, setMetadataCID] = useState('');\n\n  // Connect to wallet\n  const connectWallet = async () =&gt; {\n    const [address] = await walletClient.requestAddresses();\n    setAccount(address);\n  };\n\n  // Create order\n  const createOrder = async () =&gt; {\n    const { request } = await client.simulateContract({\n      address: contractAddress,\n      abi: ecommerceEscrowABI,\n      functionName: 'createOrder',\n      args: [seller, metadataCID],\n      value: BigInt(amount * 10 ** 18), // Convert to wei\n      account,\n    });\n    await walletClient.writeContract(request);\n    alert('Order created!');\n  };\n\n  // Fetch orders from subgraph\n  const fetchOrders = async () =&gt; {\n    const query = `\n      {\n        orders {\n          id\n          buyer\n          seller\n          amount\n          isReleased\n          isDisputed\n        }\n      }\n    `;\n    const response = await request(subgraphUrl, query);\n    setOrders(response.orders);\n  };\n\n  // Fetch metadata from IPFS\n  const fetchMetadata = async (cid) =&gt; {\n    const response = await axios.get(`https://ipfs.io/ipfs/${cid}`);\n    return response.data;\n  };\n\n  useEffect(() =&gt; {\n    fetchOrders();\n  }, []);\n\n  return (\n    &lt;div className=\"p-4\"&gt;\n      &lt;h1 className=\"text-2xl font-bold\"&gt;Ecommerce Escrow DApp&lt;/h1&gt;\n      {!account ? (\n        &lt;button onClick={connectWallet} className=\"bg-blue-500 text-white p-2 rounded\"&gt;\n          Connect Wallet\n        &lt;/button&gt;\n      ) : (\n        &lt;div&gt;\n          &lt;p&gt;Connected: {account}&lt;/p&gt;\n          &lt;div className=\"my-4\"&gt;\n            &lt;input\n              type=\"text\"\n              placeholder=\"Seller Address\"\n              value={seller}\n              onChange={(e) =&gt; setSeller(e.target.value)}\n              className=\"border p-2 mr-2\"\n            /&gt;\n            &lt;input\n              type=\"number\"\n              placeholder=\"Amount (ETH)\"\n              value={amount}\n              onChange={(e) =&gt; setAmount(e.target.value)}\n              className=\"border p-2 mr-2\"\n            /&gt;\n            &lt;input\n              type=\"text\"\n              placeholder=\"IPFS CID\"\n              value={metadataCID}\n              onChange={(e) =&gt; setMetadataCID(e.target.value)}\n              className=\"border p-2 mr-2\"\n            /&gt;\n            &lt;button onClick={createOrder} className=\"bg-green-500 text-white p-2 rounded\"&gt;\n              Create Order\n            &lt;/button&gt;\n          &lt;/div&gt;\n          &lt;h2 className=\"text-xl font-semibold\"&gt;Orders&lt;/h2&gt;\n          &lt;ul&gt;\n            {orders.map((order) =&gt; (\n              &lt;li key={order.id} className=\"my-2\"&gt;\n                Order {order.id}: {order.amount / 10 ** 18} ETH from {order.buyer} to {order.seller}\n              &lt;/li&gt;\n            ))}\n          &lt;/ul&gt;\n        &lt;/div&gt;\n      )}\n    &lt;/div&gt;\n  );\n};\n\nexport default App;\n</code></pre></div><h2><strong>Why It Matters</strong></h2><p>The frontend ties together the smart contract, subgraph, and off-chain data. Users can connect their wallets (e.g., MetaMask), create orders, and view order details fetched from the subgraph. Libraries like viem and graphql-request simplify blockchain and data interactions.</p><h1><strong>Putting It All Together</strong></h1><p>Here’s how the components work together in the e-commerce escrow DApp:</p><p>1. A user connects their wallet and enters a seller’s address, payment amount, and IPFS CID via the <strong>frontend</strong>.</p><p>2. The frontend uses <strong>viem</strong> to send a transaction to the EcommerceEscrow <strong>smart contract</strong>, calling createOrder and emitting an OrderCreated event.</p><p>3. The <strong>subgraph</strong> indexes the OrderCreated event and updates the GraphQL API with the new order data.</p><p>4. The frontend queries the subgraph using <strong>graphql-request</strong> to display the updated order list.</p><p>5. If the order includes an IPFS CID, the frontend uses <strong>axios</strong> to fetch metadata (e.g., product details) from IPFS and displays it.</p><h1><strong>Conclusion</strong></h1><p>Building a Web3 DApp involves orchestrating <strong>smart contracts</strong> for on-chain logic, <strong>The Graph</strong> for data indexing, <strong>IPFS</strong> or <strong>Web2 databases</strong> for off-chain storage, and a <strong>hybrid frontend</strong> for user interaction. By following this detailed workflow and using the provided code samples, you can create a fully functional DApp like an e-commerce escrow system.</p><h3>Next Steps</h3><p>- Deploy the EcommerceEscrow contract to Sepolia using Foundry.</p><p>- Set up a subgraph on The Graph’s hosted service.</p><p>- Upload sample product data to IPFS via Pinata.</p><p>- Build and deploy the React frontend with Vite.</p><p>- Test the DApp by creating orders and querying them via the subgraph.</p>","timestamp":"Wednesday, July 16, 2025 at 3:29:19 PM GMT+8","image":"https://storage.googleapis.com/personal-blog-darmajr.appspot.com/blog-content/1752650055841_DAppBackground.jpg","image_alt":"DApp Background Cover","slug":"Building-a-Web3-DApp-A-Detailed-Step-by-Step-Guide-with-Code-Samples","index":"d4735e3a265e1","tags":["Smart Contract","Web3","Solidity","Blockchain"]},"recommendedPosts":[]},"__N_SSG":true}